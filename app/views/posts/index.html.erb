<p id="notice"><%= notice %></p>

<h1>Posts</h1>

<table>
  <thead>
    <tr>
      <th>Titile</th>
      <th>author_name</th>
      <th>created_time</th>
      <%# <th>Content</th>
      <th>Sub forum</th>
      <th>comment_Ids</th> %>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= link_to post.title, post %></td>
        <td><%= post.sub_forum.name %></td>
        <td><%= post.create_time %></td>
        <%# <td><%= link_to 'Show', [@sub_forum] %></td>
        <%# <td><%= link_to 'Edit', edit_sub_forum_post_path(@sub_forum, post) %></td>
        <%# <td><%= link_to 'Destroy', [@sub_forum, post], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%# <%= link_to 'New Post', new_sub_forum_post_path(@sub_forum) %>